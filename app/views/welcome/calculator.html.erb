
<!-- AIzaSyCPq2xt5HMeGkVgTdZP9WhYTqkW7_avp0s -->
<div id="map"></div>
<script>

  function getLocation() {
    if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition(function(position) {
        var map = new google.maps.Map(document.getElementById('map'));
      });
    } else {
      x.innerHTML = "Geolocation is not supported by this browser.";
    }
  }

  function initMap() {
    var map = new google.maps.Map(document.getElementById('map'), {
      scrollwheel: false,
      navigationControl: false,
      mapTypeControl: false,
      scaleControl: false,
      disableDefaultUI: true,
      draggable: false,
      zoom: 18
    });


    // Try HTML5 geolocation.
    if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition(function(position) {
        var pos = {
          lat: position.coords.latitude,
          lng: position.coords.longitude
        };
        map.setCenter(pos);

        var marker = new google.maps.Marker({
          position: pos,
          map: map
        });

        var view = document.getElementById('map');
        view.style.visibility = "visible";
        Materialize.fadeInImage('#map');

      }, function() {
      });
    } else {// Browser doesn't support Geolocation
    }
  }
</script>

<script async defer
        src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBWlFra_z9JfLAxaqJ-MXmt9-RsyLJon_g&callback=initMap">
</script>
